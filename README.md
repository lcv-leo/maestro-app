<p align="center">
  <img src=".github/assets/lcv-ideas-software-logo.svg" alt="LCV Ideas &amp; Software" width="520" />
</p>

# Maestro Editorial AI

Portable Windows editorial workbench for protocol-driven AI drafting, source verification, and multi-agent editorial convergence.

[![CI](https://github.com/lcv-ideas-software/maestro-app/actions/workflows/ci.yml/badge.svg)](https://github.com/lcv-ideas-software/maestro-app/actions/workflows/ci.yml)
[![Pages](https://github.com/lcv-ideas-software/maestro-app/actions/workflows/pages.yml/badge.svg)](https://github.com/lcv-ideas-software/maestro-app/actions/workflows/pages.yml)
[![Release](https://github.com/lcv-ideas-software/maestro-app/actions/workflows/release.yml/badge.svg)](https://github.com/lcv-ideas-software/maestro-app/actions/workflows/release.yml)
![CodeQL](https://img.shields.io/badge/CodeQL-default%20setup-enabled-brightgreen)
![status](https://img.shields.io/badge/status-operational%20stable%20baseline-blue)
![target](https://img.shields.io/badge/target-Windows%2011%2B-blue)
![stack](https://img.shields.io/badge/stack-Tauri%202%20%2B%20React%2019-blueviolet)
![runtime](https://img.shields.io/badge/runtime-portable-green)
![state](https://img.shields.io/badge/state-JSON%2FNDJSON-informational)
![license](https://img.shields.io/badge/license-AGPL--3.0-blue)

**Status.** Operational stable baseline as of `v0.5.25`, with live bootstrap, diagnostics, navigation, Cloudflare credential provisioning, AI API credential checks, PostEditor parity, link auditing, and a real background Claude/Codex/Gemini/DeepSeek/Grok editorial session path. Runtime evidence from session `run-2026-05-11T01-09-30-556Z` confirms the first documented end-to-end unanimous editorial delivery: Maestro `0.5.25` resumed a real API-mode session, reached `READY_UNANIMOUS`, and wrote a clean `texto-final.md`.

Current project version: `v0.5.26`.

The version history at a glance:

| Release | Scope |
|---|---|
| **`v0.5.26`** | **Audit hardening release.** Adds atomic temp-file write/flush/rename behavior for editorial artifacts, local JSON configuration, and binary evidence attachments; classifies provider HTTP failures by operational class; strips request URLs from provider, Cloudflare, AI probe, and link-audit errors; reports partial Cloudflare Secrets Store upserts explicitly; adds a Vitest/jsdom smoke-test foundation; removes unused `sanitize-html`; and strengthens CI/release gates with `npm audit`, frontend tests, full Rust tests, and release-local gates before Windows bundle publication. Gates: `npm audit --audit-level=moderate` clean, `npm test` 2/2, `cargo check --locked --all-targets`, `cargo test --locked` 159 passed, `cargo clippy --locked --no-deps --all-targets`, one final `npm run build`, and cross-review-v2 session `4f6c2ecb-4730-4f73-9a01-8030c4a2d0e9` converged READY. |
| **`v0.5.25`** | **First operational stable baseline.** The circular scheduler no longer pauses when the nominal next reviewer is also the current text author or already approved the current version. It autonomously redraws an eligible independent pending reviewer, keeps the original redactor out of the closing turn until the independent peers have approved the stable text, and converges only after all independent reviewers approve the current version. Post-release runtime evidence from session `run-2026-05-11T01-09-30-556Z` confirms the first documented end-to-end unanimous editorial delivery: Maestro `0.5.25` resumed a real API-mode session with Claude, Codex, Gemini, DeepSeek, and Grok, reached `READY_UNANIMOUS`, and wrote a clean `texto-final.md`. Dependabot automerge concurrency now keys by PR number with a `github.ref` fallback, avoiding cross-PR cancellation. Gates: focused Rust serial-cycle tests passed (`cargo test --locked --lib serial_`, 9 passed), and cross-review-v2 session `7075ed2c-8bb0-408c-b622-fe16fbbf9ea1` converged. |
| **`v0.5.24`** | **Circular round semantics and resume-safe custody.** Maestro now keeps a round open until the current text completes the full active-agent circuit and returns to the original drafter. Resume restores the next circular turn from the latest draft/revision artifact, strict serial output validation rejects incomplete artifacts, protocol echo is compacted, and review/rewrite API calls use 20k output-token headroom. Gates: CodeQL checked clean before tests, one final `npm run build`, Rust tests/checks/clippy after CI import-scope hotfix, `git diff --check`, and cross-review-v2 sessions `a549bc27-383c-4fdb-8ca4-a6bba143f949` and `b7f845f4-0c1c-4227-a30f-bd6ace80ea04` converged. |
| **`v0.5.23`** | **Serial editorial deliberation and link-audit detail.** Maestro now moves the text through a serial reviewer-reviser cycle instead of parallel critique plus redactor rewrite: each independent peer receives the previous text, applies only protocol-grounded corrections, records an English revision report, and passes a Brazilian Portuguese final-text block onward until every independent reviewer approves the stable text. The current author is excluded from reviewing its own work, approved content is locked against scope creep, stable approval is tracked by reviewer identity, lower-tier peers are guarded against flattening stronger prose, and resume recovery accepts only `<maestro_final_text>` from revision artifacts. The evidence panel now names invalid/blocked/local/private/malformed/HTTP-failing links and explains each invalidity. Gates: CodeQL alerts checked before tests, `cargo test` 147 passed, `npm run typecheck`, one final `npm run build`, `git diff --check`, and cross-review-v2 session `09c21d7a-008f-48b1-bd48-93d93985cd43` converged READY after controlled evidence-backed peer review. |
| **`v0.5.22`** | **Recoverable reviewer operational-outage handling.** Operational-only review failures now stay operational: they do not become editorial blockers, do not trigger text revision, and do not let the current author review itself. Repeated independent-reviewer CLI/runtime outages pause recoverably as `PAUSED_REVIEWER_OPERATIONAL_OUTAGE`, preserving the current draft and telling the operator to retry reviewers, switch transport/mode, or enable more independent reviewers. Codex CLI sidecar runs now receive the full prompt via stdin, PATH resolution preserves inherited PATH while adding trusted Windows locations, and Codex/Gemini empty-output or ripgrep-missing failures get specific diagnostic statuses. Gates: full Rust lib tests, `npm run typecheck`, one final `npm run build`, and cross-review-v2 session `c63ae56a-d89f-4dd9-b56c-a3ea5a60064d` converged READY. |
| **`v0.5.21`** | **Resume/cycle provenance and operational-failure isolation.** Session contracts now preserve the original initial drafter separately from the operator-selected resume/cycle lead, so retomar com outro agente is valid without rewriting history. Repeated same-round peer calls now write append-only `-attempt-NNN` artifacts instead of overwriting previous attempts. Operational failures (`AGENT_FAILED_NO_OUTPUT`, upstream CLI/provider errors, cost blocks, operator stops) are excluded from editorial objection carry-forward; operational-only rounds skip text revision and retry review instead of causing agents to rewrite approved content. CLI stdout now strips leading Windows process-management noise before Markdown/status parsing. |
| **`v0.5.20`** | **Internal peer-language and incremental convergence contract.** Agent-to-agent coordination now uses en_US while operator-facing drafts/revisions stay in Brazilian Portuguese (pt_BR). Review prompts distinguish first-round full audit from later incremental review, carry forward unresolved non-READY objections, and enforce an approved-content lock: later rounds may reopen a passage only when the latest revision changed it, an unresolved `NOT_READY` blocker cites it, or a direct protocol-breaking defect makes final delivery unsafe. Revisions must change only passages tied to concrete blockers and return the current draft unchanged when no concrete blocker authorizes a change. Gates: focused prompt tests, full `cargo test --locked --lib`, `npm run typecheck`, one final `npm run build`, `git diff --check`, and cross-review-v2 session `1fc08967-7e5b-497e-b2fb-20f71951d9c8` unanimous READY from Claude/Gemini/DeepSeek/Grok. |
| **`v0.5.19`** | **Provider prompt-cache activation.** Direct API peers now apply cache policy without reducing model quality: OpenAI/Codex and Grok/xAI send deterministic `prompt_cache_key` values, OpenAI extended-cache-capable models request `prompt_cache_retention: "24h"`, Anthropic/Claude marks the stable system text block with `cache_control: { "type": "ephemeral" }`, DeepSeek and Gemini keep provider-side implicit prefix caching while preserving thinking behavior. Runtime artifacts, NDJSON logs, and per-session `cache-manifest.ndjson` now record non-secret cache policy hashes plus provider hit/miss/read/create token telemetry when returned. Gates include provider cache unit tests, `cargo test --locked --lib`, `npm run typecheck`, one final `npm run build`, and cross-review closure. |
| **`v0.5.18`** | **Site sponsor card iteration.** `site/index.html` GitHub Sponsors iframe (caixa branca cross-origin) substituído por link card dark navy com ❤ pink + meta cyan + seta animada; card movido para DEPOIS dos botões (lcv.dev/sponsor primário, GitHub Sponsors alternativa). Companion ship Phase 3 (12 repos). |
| **`v0.5.17`** | **Site visual identity refresh.** `site/index.html` (GitHub Pages) reskinneada para a nova identidade dark-first navy/cyan da org LCV (`#050b18`/`#38bdf8`/`#34d399`, gradientes radiais, glow shadows, gradient text no h1). Coordinated Phase 2 companion ship (calculadora, oraculo, astrologo, admin, mainsite, maestro, mtasts). Versão Tauri bumpada em package.json + src-tauri/tauri.conf.json + src-tauri/Cargo.toml + src-tauri/Cargo.lock (4 lugares). Sem mudança no app runtime. |
| **`v0.5.16`** | **Grok as the fifth editorial peer.** Grok joins Maestro as an API-only xAI peer, following the same product rule as DeepSeek: **API** runs all five peers via official providers; **Hybrid** keeps Claude/Codex/Gemini on CLI and routes DeepSeek/Grok through API; **CLI** disables DeepSeek/Grok instead of pretending a local CLI path exists. Settings now include Grok API key, Cloudflare Secrets Store persistence, provider probe, rate-card fields, attachment manifest fallback, active-peer toggles, and draft-lead selection. Backend routing adds `grok-api`, `MAESTRO_GROK_API_KEY` / `GROK_API_KEY` / `XAI_API_KEY`, xAI `/v1/models` probing, xAI `/v1/responses` execution with `store: false`, cost preflight, and no-self-review compatibility across five peers. Gates: `npm run typecheck`, `npm run build` (pre-existing Vite large-chunk warning only), `npm audit --audit-level=moderate` (0 vulnerabilities), `cargo check --locked`, `cargo test --locked grok --lib` (2 passed), `cargo test --locked should_run_agent_via_api --lib` (4 passed), `cargo test --locked selected_review_agent_specs --lib` (3 passed), `cargo test --locked` (120 passed), `cargo clippy --locked --no-deps --all-targets` (pre-existing `sanitize.rs` warning only), `git diff --check` (CRLF normalization warning only), real xAI `/v1/models` + `/v1/responses` smoke, and `cross-review-v2` session `5f4c2d44-898c-4267-9821-a72d1e1be14c` unanimous READY from Claude/Gemini/DeepSeek/Grok. |
| **`v0.5.15`** | **Tribunal-style editorial hardening.** Maestro now treats each draft/revision cycle as a collegiate editorial proceeding: the current redactor is the petitioner for that cycle, not a voting reviewer. Review panel selection fails closed when the current draft author cannot be verified, preventing resumed or malformed sessions from falling back into self-review. Draft/review/revision prompts now state the model explicitly: petitioner/redactor, independent editorial panel, votes as `READY`/`NOT_READY`, and revisions as new deliberative cycles within the same append-only case file. Gates: `npm run typecheck`, `npm run build` (pre-existing Vite large-chunk warning only), `cargo test --locked independent_review_agent_specs --lib` (3 passed), `cargo test --locked` (118 passed), `cargo clippy --locked --no-deps --all-targets` (pre-existing `sanitize.rs` warning only), `git diff --check`, and `cross-review-v2` session `b02655ca-cd23-4361-b25b-f3f673ce1ce0` unanimous READY from Claude, Gemini, and DeepSeek. |
| **`v0.5.14`** | **Resume attempt cost-scope hotfix.** Retomadas agora criam um `cost_scope_id` novo por execucao, separado do `run_id` permanente da pasta da sessao. Isso corrige o caso real em que uma sessao antiga retomada preservava o mesmo `run_id` (`run-2026-04-26T19-28-26-698Z`) e, por isso, a v0.5.13 ainda reatribuia 24 USD de historico ao limite novo de 10 USD. O `cost-ledger.json` continua preservando o historico auditavel, mas o teto da tentativa atual passa a considerar somente entradas gravadas no escopo dessa tentativa. A retomada tambem ganhou testes para garantir que o autor do rascunho/revisao recuperado dos artefatos nao entre como seu proprio revisor: em Maestro, o redator atual provoca o ciclo editorial, mas nao vota no proprio texto. Gates: `npm run typecheck`, `npm run build`, `npm audit --audit-level=moderate`, `cargo check --locked`, `cargo test --locked cost_ledger_ --lib` (3 passed), `cargo test --locked selected_review_agent_specs --lib` (3 passed), `cargo test --locked can_agent_review_current_draft --lib` (1 passed), `cargo test --locked resume_author_recovery --lib` (2 passed), `cargo test --locked` (112 passed), `cargo clippy --locked --no-deps --all-targets` (pre-existing `sanitize.rs` test-module warning only), and `cross-review-v2` session `a7f8e77a-ba3b-4d2d-82b5-472529563b62` round 4 unanimous READY. |
| **`v0.5.13`** | **Resume cost-ledger scope hotfix.** Retomadas agora calculam o custo observado apenas para o `run_id` atual, preservando entradas antigas no `cost-ledger.json` como historico, mas sem deixar custo de execucoes anteriores bloquear a nova tentativa. Entradas legadas sem `run_id` sao migradas em memoria para o `run_id` que estava gravado no ledger antigo; ledgers ainda mais antigos sem `run_id` no topo ficam como historico `__legacy_unscoped__`; novas entradas passam a persistir `run_id`. Gates: `npm run typecheck`, `npm run build`, `npm audit --audit-level=moderate`, `cargo check --locked`, `cargo test --locked` (107 passed), `cargo clippy --locked --no-deps --all-targets` (pre-existing `sanitize.rs` test-module warning only), `git diff --check`, and `cross-review-v2` session `7b63c0da-140f-4083-b8d3-bad4b76ff1a3` round 2 unanimous READY. |
| **`v0.5.12`** | **DeepSeek artifact and API cost-guard hotfix.** DeepSeek API responses now persist only final assistant `message.content` as agent stdout. Reasoning-only, empty-content, or length-truncated responses are recorded as provider failures with concise diagnostics, and raw provider JSON is no longer accepted as a draft/review artifact. DeepSeek operator cancellation now uses `STOPPED_BY_USER` with blocked tone, matching the CLI path. Any selected API peer now requires an explicit session USD cost limit before paid calls start; the UI blocks start/resume with a clear warning and the backend enforces `PAUSED_COST_LIMIT_REQUIRED` if the UI is bypassed. Gates: `npm run typecheck`, `npm run build`, `cargo check --locked`, `cargo test --locked deepseek_ --lib` (6 passed), `cargo test --locked` (105 passed), `git diff --check`, and `cross-review-v2` session `d84e5aa7-24fd-460d-97b3-f55226e10f10` round 3 unanimous READY. |
| **`v0.5.11`** | **Independent-review and diagnostics hotfix.** Review rounds now exclude the current draft/revision author, including resumed sessions whose latest author is recovered from `round-NNN-...-{draft\|revision}.md`. If no independent reviewer remains, the session pauses with `PAUSED_REVIEWERS_UNAVAILABLE` / "Sem revisor independente" instead of pretending there is a valid review cycle. API cost caps now preflight the projected cost of the whole independent review round before launching any reviewer, avoiding partial paid review rounds. Sanitized JSON keeps diagnostic field names visible while redacting secret values, resume telemetry stops reporting fake `prompt_chars: 0`/protocol metadata for saved-session prompts, and blocked-session logs store only compact recent agent summaries. Gates: `npm run typecheck`, `npm run build`, `cargo check --locked`, `cargo test --locked` (101 passed), `git diff --check`, and cross-review-v2 session `4f6cb687-2bd1-4d8a-af56-cc7aa0e76b49` recovered unanimity in round 3. |
| **`v0.5.10`** | **Artifact correctness hotfix.** `ata-da-sessao.md` now groups peer entries by the real `round-NNN-...` artifact name instead of forcing every item into `Rodada 001`, with an explicit visible bucket for unparseable artifact names. `texto-final.md` now strips the internal first-line `MAESTRO_STATUS: READY\|NOT_READY` marker when unanimity writes the public final text, including UTF-8 BOM/CRLF marker cases, while raw agent artifacts keep the protocol marker for audit/debugging. New release-close rule: after a finalized Maestro version is delivered, delete local `src-tauri/target` post-validation to keep the workspace lean. Gates: `cargo test --locked --lib` 98 passed, `npm run build` passed, and cross-review-v2 session `113c453f-57fc-477b-a647-6e52116949a5` converged READY in round 1. |
| **`v0.5.9`** | **Code split batch — frontend track. Extracted `src/types.ts` + `src/constants.ts` + `src/helpers.tsx` from `src/App.tsx`.** Pure refactor (no behavior change) per `docs/code-split-plan.md` (frontend track). 3 modules new, ~849 lines moved. **`src/types.ts`** (~253 lines, 41 type exports): all type aliases (ProtocolSnapshot, AgentState, VerbosityMode, ProviderMode, AiCredentialKey, EditorialAgentResult, EditorialSessionResult, etc.). Imports `ComponentType` from React. **`src/constants.ts`** (~287 lines, 25 const exports): initial seed values + static option tables (initialAgents, initialEvidenceRows, navGroups, settingsTabs, storageModeSummaries, aiProviderRows, providerRateRows, defaultActiveAgents, attachmentLimits, idleOperation, etc.). Imports lucide-react icons consumed by these tables. **`src/helpers.tsx`** (~309 lines, 27 function exports, .tsx extension because `stateIcon` returns JSX): pure helper functions (formatBytes, formatElapsedTime, sha256, attachmentDeliveryPlan, humanizeRunStatus, humanizeAgentStatus, latestAgentCards, summarizeAgentResults, etc.). Visibility delta is the only modification: `type X` → `export type X`, `const Y` → `export const Y`, `function Z` → `export function Z` (TypeScript equivalent of Rust `pub(crate)` upgrade). All function bodies, type structures, constant initializers, and JSX are preserved verbatim. App.tsx lucide-react import block trimmed (removed Eye, Settings, GitBranch — no longer referenced after constants.ts move; ComponentType drop from React import). Type imports trimmed to drop names not used in App.tsx body (EditorialAgentResult, NativeAttachmentProvider, RunStatus). **Out of scope (deferred per advisor's explicit boundary)**: sub-component extraction (no `<SettingsPanel />`, `<SessionHeader />`); hook refactor; useEffect/useCallback rewrites; service module extraction. The 28 preexisting aria-label warnings and 4 react-hooks/exhaustive-deps warnings in App() body predate v0.5.9 and are NOT addressed in this batch — `tsc --noEmit && vite build` passes clean. Gates: `npm run build` clean (tsc + vite production bundle, chunk shape unchanged from v0.5.8). App.tsx: 3775 → 3077 lines (−698 net). types.ts: 253 lines new. constants.ts: 287 lines new. helpers.tsx: 309 lines new. |
| **`v0.5.8`** | **Code split batch — extracted `src-tauri/src/session_orchestration.rs`.** **Largest single split since v0.4.0.** Pure refactor (no behavior change) per `docs/code-split-plan.md`. 2 functions, ~1004 lines incl. doc header. **Extracted**: `run_editorial_session_inner` (thin wrapper) and `run_editorial_session_core` (the ~925-line orchestration loop with cancel propagation, FinalizeRunningArtifactsGuard, between-rounds checks, contract resolution per the v0.5.2 / v0.3.42 invariant). Re-export shim `pub(crate) use crate::session_orchestration::{run_editorial_session_core, run_editorial_session_inner};` keeps session_commands.rs's `use crate::{...}` resolving. Visibility upgrade: `checked_data_child_path` and `sanitize_path_segment` from `crate::app_paths::*` promoted to `pub(crate) use` (sibling `session_evidence.rs` references via `crate::checked_data_child_path`); `sessions_dir` similarly `#[cfg(test)] pub(crate) use`. Massive lib.rs import cleanup: after the move, ~17 imports became unused; ~9 moved to `#[cfg(test)]`-only (filter_existing_agents_to_active_set, resolve_effective_active_agents, review_complaint_fingerprint, FinalizeRunningArtifactsGuard, build_active_agents_resolved_log_context, resolve_time_budget_anchor, load_session_contract, write_session_contract, sessions_dir), ~8 removed entirely (build_draft_prompt, build_review_prompt, build_revision_prompt, build_session_minutes, append_agent_cost_to_ledger, load_cost_ledger, parse_created_at, remaining_session_duration, session_time_exhausted, run_editorial_agent_for_spec, effective_draft_lead, provider_cost_guard_for, sanitize_optional_positive_f64, sanitize_optional_positive_u64, selected_editorial_agent_specs, process_session_evidence). `pub(crate) use crate::provider_config::*` trimmed (dropped api_provider_for_agent + provider_cost_rates_from_config + should_run_agent_via_api; kept sanitize_ai_provider_config used by tests). `pub(crate) use crate::editorial_io::*` trimmed (dropped editorial_session_result + SessionResultContext consumed only by orchestration). Gates: `cargo test --locked --lib` 93 passed (zero regressions), `cargo clippy --locked` **0 lib + 0 test warnings** (maintained baseline), `npm run build` clean. Function-body byte-parity diff vs v0.5.7 (commit `7a6a451`, lib.rs lines 612-1544 vs new session_orchestration.rs body): **clean — zero diff** (every NDJSON shape, log category, B20 comment block, RAII finalize guard, cost preflight loop, 5-phase round body preserved verbatim). lib.rs: 3072 → 2127 lines (−945 net). session_orchestration.rs: 1004 lines new. |
| **`v0.5.7`** | **Code split batch — extracted `src-tauri/src/session_commands.rs`.** Pure refactor (no behavior change) per `docs/code-split-plan.md`. 7 items, ~449 lines incl. doc header. **Tauri commands (4)**: `list_resumable_sessions`, `resume_editorial_session`, `run_editorial_session`, `stop_editorial_session`. **Blocking workers (3)**: `run_editorial_session_blocking`, `resume_editorial_session_blocking`, `list_resumable_sessions_blocking`. Visibility upgrades in lib.rs: `ResumeSessionRequest` struct + 10 fields and `EditorialSessionResult` struct + 17 fields private → `pub(crate)`; `run_editorial_session_inner` and `run_editorial_session_core` `fn` → `pub(crate) fn` (cross-module callers in session_commands.rs); the 4 Tauri commands also gained `pub(crate)` so the re-export shim feeds them into `tauri::generate_handler!`. Re-export via `use crate::session_commands::{list_resumable_sessions, resume_editorial_session, run_editorial_session, stop_editorial_session};` in lib.rs. Cleanup in lib.rs: `safe_run_id_from_entry` → `#[cfg(test)]`-only; `inspect_resumable_session_dir`/`load_resume_session_state` → `#[cfg(test)]`-only; `extract_saved_initial_agent`/`extract_saved_prompt`/`extract_saved_session_name` → `#[cfg(test)]`-only; `stable_text_fingerprint` removed entirely from lib.rs imports (only used in resume blocking). Out of scope (deferred to v0.5.8): `run_editorial_session_inner` + `run_editorial_session_core` (~954 lines orchestration with cancel propagation, FinalizeRunningArtifactsGuard, between-rounds checks, contract resolution) stay in lib.rs. Gates: `cargo test --locked --lib` 93 passed (zero regressions), `cargo clippy --locked` **0 lib + 0 test warnings** (maintained baseline), `npm run build` clean. Function-body byte-parity diff vs v0.5.6 (commit `e477ba3`): clean — only the 4 Tauri commands gained `pub(crate)` visibility; all NDJSON shapes, log categories, B22 comment block, RAII cancel guard, and resume contract resolution preserved verbatim. lib.rs: 3460 → 3072 lines (−388 net). session_commands.rs: 449 lines new. |
| **`v0.5.6`** | **Code split batch — extracted `src-tauri/src/tauri_commands.rs`.** Pure refactor (no behavior change) per `docs/code-split-plan.md`. Largest single split since v0.4.0. 11 `#[tauri::command]` items, ~341 lines incl. doc header. **Config CRUD (4)**: `read_bootstrap_config`, `write_bootstrap_config`, `read_ai_provider_config`, `write_ai_provider_config`. **Diagnostics + observability (3)**: `runtime_profile`, `write_log_event`, `diagnostics_snapshot`. **Editorial utilities (4)**: `verify_ai_provider_credentials`, `audit_links`, `open_data_file`, `run_cli_adapter_smoke`. Visibility upgrade: `RuntimeProfile` struct + 7 fields private → `pub(crate)`. Re-export shim in lib.rs preserves `tauri::generate_handler!` resolution. Massive import cleanup: `serde_json::Value` (now `#[cfg(test)]`-only), `ai_probes::run_ai_provider_probe`, `app_paths::{ai_provider_config_path, bootstrap_config_path, data_dir}`, `cli_adapter::{cli_adapter_specs, run_cli_adapter_probe}`, `link_audit::run_link_audit`, `logging::LogWriteResult`, plus 2 re-export blocks (`config_persistence` and partial `provider_config`) all removed from lib.rs. `persist_ai_provider_cloudflare_marker` re-imported `#[cfg(test)]`-gated for the existing test. Out of scope: session orchestration commands (`run_editorial_session`, `resume_editorial_session`, `list_resumable_sessions`, `stop_editorial_session`) stay in lib.rs because they are tightly coupled with their `*_blocking` helpers + `run_editorial_session_core` (~880 lines orchestration). Gates: `cargo test --locked --lib` 93 passed (zero regressions), `cargo clippy --locked` **0 lib + 0 test warnings** (maintained baseline), `npm run build` clean. Function-body byte-parity diff vs v0.5.5 (commit 0744639): clean. lib.rs: 3729 → 3460 lines (−269 net). tauri_commands.rs: 341 lines new. |
| **`v0.5.5`** | **Code split batch — extracted `src-tauri/src/cloudflare_commands.rs`.** Pure refactor (no behavior change) per `docs/code-split-plan.md`. 4 items, ~153 lines incl. doc header. **Cloudflare env probing**: `cloudflare_env_snapshot` Tauri command (probes `MAESTRO_CLOUDFLARE_ACCOUNT_ID`/`CLOUDFLARE_ACCOUNT_ID`/`CF_ACCOUNT_ID` + API_TOKEN family with HKCU/HKLM scope detection). **Settings preflight**: `dependency_preflight` (async wrapper using `spawn_blocking`) + `dependency_preflight_inner` (private; runs Claude/Codex/Gemini/Node/npm/cargo/gh `--version` checks via `command_check`, plus Cloudflare env state, plus Wrangler hint). **Cloudflare credential verifier**: `verify_cloudflare_credentials` (wraps `cloudflare::run_cloudflare_probe` with `settings.cloudflare.verify_completed` NDJSON emission). Visibility upgrade: `CloudflareEnvSnapshot` struct + 6 fields upgraded from private to `pub(crate)` for cross-module access. Re-export via `use crate::cloudflare_commands::{cloudflare_env_snapshot, dependency_preflight, verify_cloudflare_credentials};` in lib.rs so the `tauri::generate_handler!` macro in `pub fn run()` resolves the 3 command identifiers. Cleanup: `cloudflare::{run_cloudflare_probe, token_source_label}` and `command_spawn::command_check` imports removed from lib.rs (now consumed only inside `cloudflare_commands.rs`). Gates: `cargo test --locked --lib` 93 passed (zero regressions), `cargo clippy --locked` **0 lib + 0 test warnings** (maintained baseline), `npm run build` clean. Function-body byte-parity diff vs v0.5.4 (commit 812d988): clean. lib.rs: 3847 → 3729 lines (−118 net). cloudflare_commands.rs: 153 lines new. |
| **`v0.5.4`** | **Code split batch — extracted `src-tauri/src/editorial_io.rs`.** Pure refactor (no behavior change) per `docs/code-split-plan.md`. 10 items, ~256 lines incl. doc header. **File I/O**: `write_text_file`, `read_text_file` (sandboxed via `checked_data_child_path`). **Path helper**: `command_working_dir_for_output`. **Result builder**: `editorial_session_result` + `SessionResultContext<'a>` (now `pub(crate)` for cross-module access). **NDJSON loggers**: `log_editorial_agent_finished`/`spawned`/`running`. **Output parsers**: `extract_maestro_status` (MAESTRO_STATUS protocol parser), `extract_stdout_block` (fenced ## Stdout body extractor), `api_error_message` (JSON HTTP-error message extractor with sanitize_text fallback). Re-export shim in lib.rs preserves all call sites in `provider_runners.rs`, `provider_deepseek.rs`, `editorial_agent_runners.rs`, `command_spawn.rs`, plus 10 SessionResultContext construction sites in `run_editorial_session_core`. Cleanup: `CommandProgressContext` import removed from lib.rs (now used only in editorial_io.rs); `finalize_running_agent_artifacts` import gated `#[cfg(test)]`. Gates: `cargo test --locked --lib` 93 passed (zero regressions), `cargo clippy --locked` **0 lib + 0 test warnings** (maintained v0.5.3 baseline), `npm run build` clean. Function-body byte-parity diff vs v0.5.3 (commit 116154f): clean. lib.rs: 4054 → 3847 lines (−207 net). editorial_io.rs: 256 lines new. |
| **`v0.5.3`** | **Hardening pass: D sweep + items_after_test_module fix + CLI cancel artifact status refinement.** **(D)** Proactive `or_else(saved)` sweep after 4 saved-contract leak bugs in sequence — all 5 remaining `or_else` patterns in lib.rs are intentional content-not-config (prompt, session_name, protocol_text, effective_initial_agent already-fixed-correctly, links). Pattern fully consolidated, no fixes needed. **(B1)** `pub fn run()` (Tauri 2 binary entry, 88 lines) moved before `mod tests` in lib.rs. Resolves Gemini's last clippy warning `items_after_test_module`. Function body byte-identical; only position changed. **(B2)** [src-tauri/src/editorial_agent_runners.rs](src-tauri/src/editorial_agent_runners.rs) `run_editorial_agent` now detects operator-cancel via `result.timed_out && cancel_token.is_cancelled()` and classifies the agent artifact as status `STOPPED_BY_USER` (tone `blocked`) instead of generic `EMPTY_DRAFT`/`AGENT_FAILED_NO_OUTPUT`. Closes deepseek follow-up #1 from v0.5.0 R1. Differentiates real timeouts from operator-stops at agent artifact level (session-level was already correct via the between-rounds check). **Cleanup**: removed duplicate `#[allow(clippy::too_many_arguments)]` on `run_editorial_agent` (introduced during v0.5.0 cancel plumbing). Patch bump (v0.5.2 → v0.5.3). Gates: `cargo test --locked --lib` 93 passed, `cargo clippy --locked` **0 lib + 0 test warnings** (down from 1 lib + 1 test warning in v0.5.2; both warnings resolved), `npm run build` clean. |
| **`v0.5.2`** | **B22 fix — resume path was carrying initial_agent + cost + minutes forward from saved contract over operator's request.** Operator-reported: "Eu não defini tempo nem custos na última sessão, mas ele encerrou com tempo e custos definidos em outra sessão." Empirical evidence in log `maestro-2026-05-02T20-06-37Z-pid13460.ndjson`: frontend correctly sent `requested_max_session_cost_usd: None, requested_max_session_minutes: None`, but backend reported `max_session_cost_usd_requested: 5.0, max_session_cost_usd_source: "request"` (mislabeled — value was silently substituted from saved_contract before the resolver saw it). Session killed with `COST_LIMIT_REACHED` at 4.91 USD against the silent 5.0 USD cap from a prior session. **Two separate bugs** in [resume_editorial_session_blocking](src-tauri/src/lib.rs): (1) `effective_initial_agent = saved.or_else(request)` had inverted priority — saved wins over operator's UI choice. (2) `request.max_session_cost_usd.or_else(saved...)` and `.max_session_minutes.or_else(saved...)` were the EXACT `or_else` pattern that v0.3.42 B20 fix removed from `run_editorial_session_blocking`, but the resume path was overlooked at the time. **Fix**: flipped initial_agent priority to `request.or_else(saved)`; removed `or_else(saved)` for `active_agents`, `max_session_cost_usd`, `max_session_minutes` in the resume path. Kept `links` saved fallback because links is content (not config caps). **Pattern consolidation**: with v0.5.2 all three layers (frontend, start path, resume path) now share the same semantics — request is source of truth for all CONFIG fields (peers, initial_agent, cost cap, minutes cap); saved is reference only. Mirrors and completes v0.3.42 B20 + v0.5.1 B21. Patch bump (v0.5.1 → v0.5.2). Gates: `cargo test --locked --lib` 93 passed, `npm run build` clean. |
| **`v0.5.1`** | **B21 fix — resume now honors operator's current React state instead of silently overriding with saved session contract.** Operator-reported: "maestro-app importa os peers anteriormente configurados, não respeitando novas configurações." Root cause in [src/App.tsx](src/App.tsx) `startResumeSession`: called `setActiveAgents(validSavedAgents)` + `setInitialAgent(resolvedInitial)` at resume time, overriding whatever the operator had in the UI. Backend correctly honored `request.active_agents` as source of truth (per v0.3.15 B11 + `resolve_effective_active_agents` resolver), but the "request" was being built from saved-contract values instead of current UI state. This was the v0.3.18 B17 fix's behavior (auto-pre-populate from saved on resume so cold-open continues with same peers) which broke the more common scenario of operator deliberately changing peer config. **Fix**: removed `setActiveAgents` and `setInitialAgent` overrides; removed the `if-validSavedAgents` branch; always use `currentSessionRunOptions()` to build `resumeRunOptions`. `saved_active_agents`/`saved_initial_agent` fields stay in `ResumableSessionInfo` for informational picker UI display but are NEVER auto-applied. New `session.resume.contract_applied` NDJSON event records both `saved_*` (informational) and `requested_*` (what's actually used) for audit. Mirrors v0.3.42 B20 fix for caps: **request is source of truth; saved_contract is reference only**. Both peer selection and caps now follow the same semantics. Patch bump (v0.5.0 → v0.5.1) — pure bugfix, no signature/dep/feature changes. Gates: `cargo test --locked --lib` 93 passed (existing test `resolve_effective_active_agents_request_overrides_saved` already covers the backend invariant that this frontend fix relies on); `npm run build` clean. |
| **`v0.5.0`** | **Operator-driven session stop with sub-2-second cancel granularity.** New "Parar sessão" button in [src/App.tsx](src/App.tsx) header (visible while session running, disabled after click until session ends, lucide Square icon, confirmation dialog before invoke). New Tauri command `stop_editorial_session(run_id)` registered alongside existing session commands. New backend module [src-tauri/src/session_cancel.rs](src-tauri/src/session_cancel.rs) (~140 lines) with `OnceLock<Mutex<HashMap<String, CancellationToken>>>` keyed registry + `CancelTokenGuard` RAII (5 unit tests cover idempotency + panic-safe Drop + unknown-run_id graceful return). [src-tauri/src/provider_retry.rs](src-tauri/src/provider_retry.rs) gains async sibling `send_with_retry_async` wrapping `request.send().await` in `tokio::select! { biased; _ = cancel_token.cancelled() => Cancelled, r = future => r }`; new `ProviderRequestOutcome::{Cancelled, Network}` enum lets runners distinguish operator-stop from network failures. All 4 API runners (`run_openai_api_agent`, `run_anthropic_api_agent`, `run_gemini_api_agent`, `run_deepseek_api_agent`) migrated to `pub(crate) async fn (request, cancel_token)` and use 2 clients per spawn (blocking for short `/models` resolve probe + async for the main cancel-aware editorial request). Orchestration: [src-tauri/src/editorial_agent_runners.rs](src-tauri/src/editorial_agent_runners.rs) `run_provider_api_agent` uses `tauri::async_runtime::block_on` to bridge sync session loop → async runners (safe because session runs in `spawn_blocking` worker, no nested-runtime risk). [src-tauri/src/command_spawn.rs](src-tauri/src/command_spawn.rs) `run_resolved_command_observed` accepts `Option<&CancellationToken>` and the existing 250ms poll loop checks `is_cancelled()` → `kill_process_tree` (Windows: `taskkill /T /F /PID`) → returns partial output as `timed_out: true`. Between-rounds cancel check in `run_editorial_session_core` emits `session.user.stop_completed` and returns status `STOPPED_BY_USER`. New deps: `tokio = { version = "1", default-features = false, features = ["macros", "time", "sync", "rt"] }`, `tokio-util = "0.7"`. **Operator workflow**: click → CLI peer killed in ≤250ms, API peer dropped in <2s, session exits with STOPPED_BY_USER, artifacts preserved, resume via standard "Retomar" flow. Validated against real operator log `run-2026-05-02T11-39-41-113Z` showing 35min session with peers running 3-7min each that ended only by killing the app. Sync `send_with_retry` deleted (all callers migrated). Gates: cargo test --locked --lib 93 passed (88 + 5 new), cargo clippy --locked same shape as v0.4.0 (only `items_after_test_module` cosmetic warning remains), npm build clean. Versioning: minor bump for new user-facing feature + new IPC surface + dep additions; NOT a breaking change. |
| **`v0.4.0`** | **Architectural refactor closing Gemini audit's `too_many_arguments` × 7 finding.** Two new `pub(crate)` context structs in [src-tauri/src/provider_runners.rs](src-tauri/src/provider_runners.rs) collapse 9–11 positional parameters into 1–2 grouped arguments. **`ProviderInvocation<'a>`** (Clone+Copy, 7 fields: `log_session`, `run_id`, `name`, `cli`, `provider`, `role`, `output_path`) — built once inside each runner from its hardcoded `name`/`cli`/`provider` constants plus the request's spawn context. **`EditorialAgentRequest<'a>`** (9 fields, owns `prompt: String` and `cost_guard: Option<ProviderCostGuard>`) — the per-call bundle each runner accepts. Refactored: 4 helpers (`api_cost_preflight_result` 11→5 args; `write_provider_missing_key_result` 9→3 args; `write_provider_error_result` 10→4 args; `write_provider_failure_result` 13→7 args, no longer needs `#[allow]`); 4 runners (`run_openai_api_agent`, `run_anthropic_api_agent`, `run_gemini_api_agent`, `run_deepseek_api_agent` — all 9→1 arg, take `EditorialAgentRequest`). Single dispatch point at [editorial_agent_runners.rs:run_provider_api_agent](src-tauri/src/editorial_agent_runners.rs) builds the request once and `match spec.key` moves it into the matching runner; fallback constructs inline `ProviderInvocation` with `provider: "unknown"`. Out of scope: `write_provider_success_result` (17 args, retains `#[allow]` — would need a third "outcome" struct), `log_provider_api_started` (11 args, retains `#[allow]`), `write_deepseek_error_result` (7 args, no warning), `items_after_test_module` (cosmetic, resolves naturally as splits continue). **Versioning**: minor bump (v0.3.48 → v0.4.0) for architectural milestone; NOT a breaking change for end users — function bodies are byte-identical to v0.3.48 modulo signature swaps. Gates: `cargo test --locked --lib` 88 passed (zero regressions), `cargo clippy --locked --no-deps --all-targets` **0 lib warnings + 1 test warning** (down from 7 + 8 in v0.3.48; **all 7 `too_many_arguments` eliminated**), `npm run build` clean. |
| **`v0.3.48`** | **Code split batch — extracted `src-tauri/src/app_init.rs`.** Pure refactor (no behavior change). App boot helpers extracted from lib.rs per `docs/code-split-plan.md` migration step 2 tail (deferred from v0.3.17): 5 items, ~138 lines incl. doc header. `initialize_app_root` (Tauri `setup` hook → `app_paths::resolve_portable_app_root` + `try_set_app_root`), `install_process_panic_hook` (forwards panics to `write_early_crash_record` so crash trail exists even before normal NDJSON logger startup), `write_early_crash_record` (writes `data/logs/maestro-crash-<timestamp>-pid<pid>.json` with cap-limited payload + location), `hidden_command` (SAFE-FUNNEL `Command::new` allowed by `clippy.toml` `disallowed-methods` — always passes through `apply_hidden_window_policy`), `apply_hidden_window_policy` (Windows-only `creation_flags(CREATE_NO_WINDOW = 0x08000000)`; no-op elsewhere). Re-export shim in lib.rs preserves call sites in `pub fn run()` + sibling modules; `write_early_crash_record` `#[cfg(test)]`-gated for the existing `tests::writes_early_crash_record_before_normal_logger` invariant. Cleanup: `app_paths` import block trimmed in lib.rs (4 helpers moved to `app_init.rs`); `std::process::{self, Command, Output}` collapsed to `std::process::Output`. `pub fn run()` Tauri entry stays in lib.rs (prefers `#[cfg_attr(mobile, tauri::mobile_entry_point)]` location). Gates: `cargo test --locked --lib` 88 passed (zero regressions), `cargo clippy --locked` same shape as v0.3.47 (7 lib + 8 test warnings, no new), `npm run build` clean. Function-body byte-parity diff vs v0.3.47 (commit 4b56e0d): clean. lib.rs: 4035 → ~3942 lines (−93 net). |
| **`v0.3.47`** | **Bundled hardening pass closing the remaining P1 set from Codex's audit (#2 DOCX/paste sanitization + Tauri webview CSP allowlist; #3 Cloudflare Secrets Store mode UI clarification) plus 5 of 7 trivial clippy hygiene fixes from Gemini's audit.** **(1) DOCX / paste sanitization**: [PostEditor.tsx](src/editor/posteditor/PostEditor.tsx) `handleWordUpload` now passes Mammoth output through `DOMPurify.sanitize(htmlResult.value, { ADD_ATTR: ['style', 'data-width'] })` before insert; [extensions.ts](src/editor/posteditor/editor/extensions.ts) `WordPasteHandler.transformPastedHTML` final return is now `DOMPurify.sanitize(clean, ...)`. Existing Mso/xmlns/comment-strip layer preserved; DOMPurify is the defense-in-depth pass dropping event handlers, javascript: URLs, embedded SVG with scripts. Markdown import already used DOMPurify since v0.3.x — this brings DOCX + paste paths to parity. **(2) Tauri webview CSP allowlist**: [tauri.conf.json](src-tauri/tauri.conf.json) `app.security.csp` is no longer `null`. Baseline `default-src 'self'` with `script-src 'self'` (NO `unsafe-inline` — actual XSS hardening), `style-src 'self' 'unsafe-inline'` (Tiptap requires inline styles for TextStyle/Color/FontFamily/TextIndent/EditorSpacing), `img-src/media-src 'self' data: blob: https:` (data URIs + arbitrary HTTPS image URLs), `frame-src` allowlist for YouTube nocookie embeds, `connect-src` for the 4 provider APIs + Cloudflare + Tauri IPC. **(3) Cloudflare Secrets Store mode UI clarification**: [App.tsx:358](src/App.tsx#L358) row detail and [App.tsx:370-373](src/App.tsx#L370-L373) `storageModeSummaries.cloudflare` rewritten to explain that the app does NOT fetch secrets from Cloudflare at runtime; local execution still needs `MAESTRO_*_API_KEY` env vars. Cloudflare mode is a canonical-storage choice, not a path that unlocks local execution by itself (cheap option (a) from the operator decision matrix). **(4) 5 trivial clippy fixes**: `needless_return` in app_paths.rs:48; `manual_is_multiple_of` in human_logs.rs:115 (`!elapsed.is_multiple_of(300)`); `manual_div_ceil` in session_evidence.rs:400 (`(size as usize).div_ceil(3) * 4`); 3× `ptr_arg` in config_persistence.rs (`&PathBuf` → `&Path` for `persist_bootstrap_config` / `persist_ai_provider_config` / `persist_ai_provider_cloudflare_marker`); `needless_borrows_for_generic_args` in lib.rs:3245. Out of scope: `too_many_arguments` × 7 in provider_runners.rs + provider_deepseek.rs (architectural refactor for v0.4.0 with `EditorialAgentContext` struct), `items_after_test_module` in lib.rs:2542 (87-line `pub fn run()` move; will resolve naturally as lib.rs splits continue per `docs/code-split-plan.md`), Codex #6 hardcoded model lists (low-priority drift), biome.json (not in pipeline). Gates: `cargo test --locked --lib` 88 passed (zero regressions), `cargo clippy --locked` 7 lib + 8 test warnings (down from 11 + 14), `npm run build` clean. |
| **`v0.3.46`** | **Hardening pass closing P0 set from Codex's read-only audit on HEAD `2ca92e7` (v0.3.45).** Three defensive fixes + the CI gate that would have caught the v0.3.45 lockfile drift. **(1) Rust reproducibility gate**: regenerated [src-tauri/Cargo.lock](src-tauri/Cargo.lock) (v0.3.45 shipped pinned to `0.3.44` because no Rust gate in CI surfaced the drift; `cargo check --locked` failed locally). New `rust-gates` job in [.github/workflows/ci.yml](.github/workflows/ci.yml) on `windows-latest` runs `cargo check --locked --all-targets`, `cargo test --locked --lib`, `cargo clippy --locked --no-deps --all-targets`. [.github/workflows/release.yml](.github/workflows/release.yml) tauri build now passes `-- --locked` to cargo. **(2) Pipe buffer cap**: new `pub(crate) const MAX_PIPE_BYTES: u64 = 64 * 1024 * 1024;` in [src-tauri/src/command_spawn.rs](src-tauri/src/command_spawn.rs). Past the cap, bytes continue to drain from the OS pipe (so the child does not block and the timeout branch can still reap it cleanly) but are not retained; new `stdout_truncated_oversize` marker surfaces the cap value in the artifact and takes precedence over a late I/O error. **(3) Process tree teardown**: new `pub(crate) fn kill_process_tree(child: &mut Child)` runs `taskkill /T /F /PID <id>` on Windows before falling back to `child.kill()` — fixes grandchild leak when peers are reached through `cmd.exe /C <peer>.cmd` (`.cmd`/`.bat` resolves) or `powershell.exe -File` (`.ps1` resolves). Two call sites swapped (stdin-write failure + timeout-elapsed branches). 5 new `#[cfg(test)]` invariants pin the cap behavior + classifier semantics + drainage past the cap. Out of scope: #2 DOCX/paste sanitizer + CSP allowlist (deferred to v0.3.47), #3 Cloudflare Secrets Store dead-end (deferred pending operator decision on UI clarification vs runtime fetch), #6 hardcoded model lists (low-priority drift). Gates: `cargo test --locked --lib` 88 passed (83 + 5 new), `cargo clippy --locked` same shape as v0.3.45 (no new warnings), `npm run build` clean. |
| **`v0.3.45`** | **Two operator-directed changes.** (1) Default editorial prompt rewritten — [src/App.tsx:821](src/App.tsx#L821) replaces `'Escreva um artigo academico sobre o tema informado, seguindo integralmente o protocolo editorial ativo.'` with `'Escreva um artigo acadêmico sobre [...], seguindo rigorosa e integralmente o protocolo editorial ativo.'` (proper accent on `acadêmico`, explicit `[...]` slot for the topic, stronger `rigorosa e integralmente` wording). (2) Upstream-bug stderr classification — operator catch on session log `maestro-2026-05-02T11-33-55Z-pid27440` (v0.3.43 binary): "vários erros silenciosos em background" was the same incident from 4 different upstream failures. Maestro now classifies Codex stderr containing `ConstrainedLanguage`/`Cannot set property`/`ERRO: o processo` as **`CODEX_WINDOWS_SANDBOX_UPSTREAM`** (Codex CLI 0.128.0+ Windows ConstrainedLanguage taskkill bug — documented upstream, deferred from cross-review-mcp v1.4.0→v1.5.0+) and Gemini stderr containing `Path not in workspace`/`resolves outside the allowed workspace directories` as **`GEMINI_WORKSPACE_VIOLATION`** (CLI `--skip-trust` blocks paths outside the agent CWD). Out of scope: DeepSeek silent empty response (provider-side; no fingerprint to detect) and Claude CLI exit 1 with empty stderr (truly silent; stays generic). 5 new Rust tests pin the classification matrix. `cargo test`: 83 passed (78 + 5 new). |
| **`v0.3.44`** | **Code split batch — extracted `src-tauri/src/api_payloads.rs`.** Pure refactor (no behavior change). Per-provider API request payload builders + native attachment support detection: 11 items (1 const + 4 pub(crate) fns + 6 module-private fns), ~214 lines. `API_NATIVE_ATTACHMENT_MAX_FILE_BYTES` (20 MiB inline-payload cap), `api_input_estimate_chars` (input-cost preflight estimator), `provider_supports_native_attachment` dispatcher + per-provider `*_api_attachment_supported` helpers (openai/anthropic/gemini + openai_file_*) gated by payload cap, `attachment_within_native_payload_cap`, and the 3 request shape builders: `openai_api_input` (Responses API: input_text/input_image/input_file), `anthropic_api_user_content` (Messages API: text/image-base64/document), `gemini_api_user_parts` (generateContent: text + inline_data). Re-export shim in lib.rs preserves call sites in `provider_runners.rs` and `provider_deepseek.rs`; the constant is `#[cfg(test)]`-gated. Cleanup: trimmed `session_evidence` import block 13→3 in lib.rs (10 attachment helpers moved to api_payloads.rs); removed orphaned `filter_existing_agents_to_active_set` doc comment from lib.rs (that fn lives in editorial_helpers.rs since v0.3.24). lib.rs: 4177 → 4049 lines (−128 net). Clippy 11 lib + 14 test warnings (one fewer than v0.3.43 — orphaned-doc removal). |
| **`v0.3.43`** | **Code split batch — extracted `src-tauri/src/provider_routing.rs`.** Pure refactor (no behavior change). Per-agent provider routing helpers + env-var lookup chain: 8 functions, ~185 lines. `api_cli_for_agent` (agent_key → "anthropic-api" etc. for NDJSON `cli` fields), `provider_label_for_agent` (agent_key → "Anthropic / Claude" for UI), `provider_remote_present` (Cloudflare-managed credential check via `*_api_key_remote` flags), `provider_key_for_agent` (config first, then env fallback), `first_env_value` (candidate-list walker returning name/scope/value), `env_value_with_scope` (process env → Windows HKCU → Windows HKLM via reg.exe), `windows_registry_env_value` (module-private reg.exe parser), `effective_provider_key` (config trimmed wins, else env candidates). Re-export shim in lib.rs preserves call sites in 6 sibling modules (`ai_probes.rs`, `cloudflare.rs`, `editorial_agent_runners.rs`, `provider_config.rs`, `provider_deepseek.rs`, `provider_runners.rs`). `hidden_command` and `AiProviderConfig` stayed in lib.rs. lib.rs: 4297 → 4177 lines (−120 net). |
| **`v0.3.42`** | **Behavior fix — B20 backend completion: saved cost/time caps were being silently re-applied on resume even when the operator's form was blank.** Operator catch on log `maestro-2026-05-02T10-54-07Z-pid1688.ndjson` (running v0.3.39 binary): with form blank on resume, DeepSeek spawned with `cost_limit_usd: 5.0` because [src-tauri/src/lib.rs:1575-1586](src-tauri/src/lib.rs#L1575-L1586) used `request.max_session_cost_usd.or_else(\|\| saved_contract...)` — the v0.3.32 B20 fix corrected the frontend (no pre-population) but never removed the backend `or_else` fallback. **All releases v0.3.32–v0.3.41 carry this regression.** Fix: replaced the `or_else` fallback with direct request pass-through (`sanitize_optional_positive_f64(request.max_session_cost_usd)`). Per the 2026-05-02 operator directive, the request alone is source of truth — None means "no cap" and saved contract values are historical reference only. Provider mode toggle (Hibrido/CLI/API) re-verified working correctly in the same log (DeepSeek→API, others→CLI in hybrid). Gates: `cargo test --lib` 78 passed, `npm run build` clean. Pure behavior fix; cli/provider routing split deferred. |
| **`v0.3.41`** | **Code split batch — extracted `src-tauri/src/config_persistence.rs`.** Pure refactor (no behavior change). Bootstrap + AI provider config persistence (disk + Cloudflare): 7 functions, ~311 lines incl. doc header. `persist_bootstrap_config` / `persist_ai_provider_config` (atomic JSON write via `checked_data_child_path`), `persist_ai_provider_cloudflare_marker` (clears API keys, records remote presence + store id/name on disk), `persist_ai_provider_config_to_cloudflare` (full upload: ensure D1 + Secrets Store, upsert secrets, write metadata row), `enrich_ai_provider_config_from_cloudflare` (best-effort merge of remote flags/store id+name into local config), `read_ai_provider_cloudflare_metadata` (reads `maestro_settings WHERE key='ai.providers'` from D1, rebuilds AiProviderConfig with cloudflare mode/secrets/tariffs), `json_find_first_string` (module-private recursive serde_json helper). `pub(crate) struct BootstrapConfig` + 9 fields upgraded for cross-module access. Re-export shim in `lib.rs` preserves all unqualified call sites. Cleanup: trimmed cloudflare import block 10→2 in lib.rs (8 helpers now live in `config_persistence.rs`); `ai_provider_secret_values` `#[cfg(test)]`-gated in lib.rs. lib.rs: 4527 → 4295 lines (−232 net). |
| **`v0.3.40`** | **Code split batch — extracted `src-tauri/src/provider_config.rs`.** Pure refactor (no behavior change). AI provider config validation, sanitization, mode-normalization, and routing layer: 11 functions, ~340 lines incl. doc header + 5 tests. `normalize_storage_mode` / `normalize_provider_mode` / `normalize_cloudflare_token_source` (closed-enum collapsers), `sanitize_optional_secret` / `sanitize_optional_cost_rate`, `sanitize_ai_provider_config` (full per-field config builder), `merge_ai_provider_env_values` + `provider_env_value` (env var fallback for absent API keys), `provider_cost_rates_from_config` (per-agent cost-guard preflight), `api_provider_for_agent` (agent_key → provider_label mapping), `should_run_agent_via_api` (the v0.3.38 routing decision: deterministic-by-identity hybrid). 5 unit tests migrated verbatim (4 v0.3.38 mode-routing invariants + `ai_provider_config_trims_empty_secret_fields`). Re-export shim in `lib.rs` (`pub(crate) use crate::provider_config::{...}`) keeps `crate::sanitize_ai_provider_config` and similar unqualified call sites working — same pattern as v0.3.34 `sanitize.rs`. Items consumed exclusively inside `provider_config.rs` are NOT re-exported (minimal surface). `ProviderCostRates` import in lib.rs is now `#[cfg(test)]`-gated. lib.rs: 4788 → 4527 lines (−261 net). |
| **`v0.3.39`** | **Code split batch — extracted `src-tauri/src/cli_adapter.rs`.** Pure refactor (no behavior change). CLI adapter smoke probe machinery: 2 functions, ~111 lines. `cli_adapter_specs` (3-element spec table for Claude/Codex/Gemini with per-CLI args + 90s timeout), `run_cli_adapter_probe` (single-spec runner with PATH resolution + outcome classification: timeout/ok+marker/ok-without-marker/nonzero-exit). `pub(crate)` upgrades on 4 structs (`CliAdapterSmokeRequest` + 5 fields, `CliAdapterSmokeResult` + 3 fields, `CliAdapterProbeResult` + 7 fields, `CliAdapterSpec` + 5 fields) for cross-module access. `run_cli_adapter_smoke` Tauri command wrapper stays in lib.rs (registry boundary + thread orchestration). Removed now-unused `Instant` and `run_resolved_command_with_timeout` imports from lib.rs. Function-body byte-parity diff vs v0.3.38 clean. lib.rs: 4902 → 4788 lines (−114 net). |
| **`v0.3.38`** | **Behavior fix — provider mode toggle (Hibrido/CLI/API) was being ignored for DeepSeek.** Operator catch on session log `maestro-2026-05-02T09-01-33Z`: with `provider_mode='cli'` selected, DeepSeek still ran via `https://api.deepseek.com/chat/completions` while Claude/Codex/Gemini correctly resolved to local CLI binaries. Root cause: both backend [src-tauri/src/lib.rs:1664](src-tauri/src/lib.rs#L1664) `should_run_agent_via_api` and frontend [src/App.tsx:926](src/App.tsx#L926) `agentUsesApi` short-circuited DeepSeek to API regardless of mode (workaround for the absence of a DeepSeek CLI integration in maestro-app). Fix: provider mode is now strict and deterministic — **API**: all 4 peers via API; **Hybrid**: DeepSeek via API + Claude/Codex/Gemini via CLI always (regardless of which API keys are configured); **CLI**: all CLI with the DeepSeek button disabled in agent toggles ("DeepSeek so roda via API. Troque para Hibrido ou API para incluir."). Frontend: `chooseProviderMode` auto-evicts DeepSeek from `activeAgents`/`initialAgent` when switching to CLI; new `useEffect([providerMode])` defense-in-depth catches config-load and resume paths. Settings UI text rewritten to explain the new semantics explicitly. 4 new `#[test]` invariants pin the per-mode routing matrix. `cargo test`: 78 passed (74 + 4 new). `npm run build`: clean. Cli adapter split (`cli_adapter.rs`) deferred to v0.3.39. |
| **`v0.3.37`** | **Code split batch — extracted `src-tauri/src/session_minutes.rs`.** Pure refactor (no behavior change). `ata-da-sessao.md` (session minutes) text generators: 2 functions, ~115 lines. `build_session_minutes` (markdown body with header/per-agent bullets/"Decisao" section), `build_blocked_minutes_decision` (non-unanimous explanation: READY reviews / operational failures / editorial divergences counts + last-8 listings). Removed unused `all_agent_keys` import from lib.rs. ZERO-line diff. lib.rs: 4956 → 4836. |
| **`v0.3.36`** | **Code split batch — extracted `src-tauri/src/editorial_agent_runners.rs`.** Pure refactor (no behavior change). Per-spec agent dispatchers + CLI-path editorial agent runner: 3 functions, ~353 lines (2 sed ranges). `run_editorial_agent_for_spec` (API peer vs CLI dispatcher), `run_provider_api_agent` (match-by-key dispatcher to 4 API peer runners with `API_PROVIDER_NOT_SUPPORTED` fallback), `run_editorial_agent` (CLI-path runner with sidecar input prep >48 KB, CLI_NOT_FOUND short-circuit, RUNNING placeholder, spawn-via-`run_resolved_command_observed`, status classification, artifact emission). Visibility upgrade in lib.rs: `api_cli_for_agent` upgraded to `pub(crate)`. 2-range byte-parity diff exit=0 after stripping defensive `#[allow(clippy::too_many_arguments)]`. lib.rs: 5302 → 4956. |
| **`v0.3.35`** | **Code split batch — extracted `src-tauri/src/command_spawn.rs`.** Pure refactor (no behavior change). Child-process spawn machinery: 8 items, ~282 lines. `command_check` (dependency_preflight diagnostic), `CommandProgressContext` (per-spawn log fields), `run_resolved_command_with_timeout`/`run_resolved_command_observed` (spawn loop with 250ms poll, 30s progress emit, optional timeout), `read_pipe_to_end_counting_classified` (atomic byte counter), `classify_pipe_error` (Windows raw_os_error 109/232/233 + std `ErrorKind`), `resolved_command_builder` (Windows .cmd/.bat → `cmd.exe /C`; .ps1 → `powershell.exe -NoProfile -ExecutionPolicy Bypass -File`), `apply_editorial_agent_environment` (UTF-8 envs + `GEMINI_CLI_TRUST_WORKSPACE`). Visibility upgrades in lib.rs: `TimedCommandOutput` + 5 fields; `hidden_command`, `command_working_dir_for_output`, `log_editorial_agent_spawned`, `log_editorial_agent_running` upgraded to `pub(crate)`. ZERO-line byte-parity diff. lib.rs: 5581 → 5302. |
| **`v0.3.34`** | **Code split batch — extracted `src-tauri/src/sanitize.rs`.** Pure refactor (no behavior change). Foundational text-sanitization + secret-redaction helpers (7 items, ~132 lines): `sanitize_short`, `sanitize_text`, `truncate_text_head_tail`, `sanitize_value`, `should_redact_key`, `redact_secrets`, `secret_value_regex`. Re-export shim `pub(crate) use crate::sanitize::{...}` in lib.rs preserves the existing `crate::sanitize_text` import path across all 19 sibling modules — zero downstream `use` changes required. Removed unused `regex::Regex` and `std::sync::OnceLock` imports from lib.rs. ZERO-line byte-parity diff. lib.rs: 5712 → 5581. |
| **`v0.3.33`** | **Code split batch — extracted `src-tauri/src/command_path.rs`.** Pure refactor. PATH-resolution helpers for child command spawn (3 fns, ~73 lines): `resolve_command`, `command_candidate_paths` (Windows extension expansion `[.exe, .cmd, .bat, .ps1, ""]`), `command_search_dirs` (process PATH + Windows install locations: `USERPROFILE\.cargo\bin`, `APPDATA\npm`, `LOCALAPPDATA\Programs\nodejs`, `LOCALAPPDATA\Microsoft\WinGet\Links`, `C:\Program Files\nodejs`, `C:\nvm4w\nodejs`, `C:\Program Files\GitHub CLI`). Spawn machinery (`run_resolved_command_*`, `read_pipe_*`, `classify_pipe_error`, `resolved_command_builder`, `apply_editorial_agent_environment`, `command_check`) stayed in lib.rs — tightly coupled with `CommandProgressContext` / `TimedCommandOutput` / `log_editorial_agent_*`. ZERO-line diff. lib.rs: 5783 → 5712. |
| **`v0.3.32`** | **B20 fix — resume should NOT carry forward saved cost/time caps.** Operator-reported behavior bug 2026-05-02: "Quando a sessão é retomada, ele traz de volta o tempo e o custo configurados na sessão anterior. Cada nova sessão, mesmo que seja sessão retomada, deve ser livre para que o usuário defina novos valores ou não." Root cause: v0.3.18 B17 fix bundled cost/minutes pre-population alongside peer pre-population in `startResumeSession`. Fix: removed `setMaxSessionMinutes`/`setMaxSessionCostUsd` calls reading `session.saved_max_session_*` and removed cap injection from saved contract into `resumeRunOptions`. Now reads cost/minutes from `currentSessionRunOptions()` (operator's current UI input — empty = unlimited). Peer continuity (`active_agents`/`initial_agent`) still pre-populated from saved contract (intended B17 behavior preserved). Cross-review-v2 quadrilateral unanimous_ready (session `174eeeb4`). |
| **`v0.3.31`** | **Code split batch — extracted `src-tauri/src/link_audit.rs`.** Pure refactor. Public-URL extraction + audit (HEAD/GET probe + private-network IP blocklist) — 9 fns, ~193 lines: `run_link_audit`, `extract_public_urls`, `is_public_http_url`, `is_blocked_link_audit_ip(_ipv4\|_ipv6)` (RFC 1918/6890/4193/5737/6598 + IPv6 reserved/link-local/ULA/multicast), `probe_public_url(_with_get)`, `link_audit_row`. Rerouted `is_public_http_url` import in `session_evidence.rs`. ZERO-line diff. lib.rs: 5973 → 5783. |
| **`v0.3.30`** | **Code split batch — extracted `src-tauri/src/ai_probes.rs`.** Pure refactor. AI provider credential probes (4 providers + helpers, 8 fns, ~164 lines): `run_ai_provider_probe` + `probe_openai_api`/`probe_anthropic_api`/`probe_gemini_api`/`probe_deepseek_api` + `missing_provider_key_row`/`summarize_ai_probe_response`/`ai_probe_row`. ZERO-line diff. lib.rs: 6137 → 5973. |
| **`v0.3.29`** | **Code split batch — extracted `src-tauri/src/session_artifacts.rs`.** Pure refactor. Resumable-session inspection + agent-runs/* artifact reading (9 fns, ~261 lines): `inspect_resumable_session_dir`, `load_resume_session_state`, `find_latest_draft_artifact(_from_artifacts)`, `artifact_resume_rank`, `load_agent_results_from_dir`, `read_agent_artifacts`, `parse_agent_artifact_name(_result)`. `SessionArtifact` struct stays in lib.rs (consumed by both `session_artifacts.rs` and `session_resume.rs`). ZERO-line diff. lib.rs: 6398 → 6137. |
| **`v0.3.28`** | **Code split batch — extracted `src-tauri/src/session_resume.rs`.** Pure refactor. Session-time helpers + extract/parse utilities + protocol-backup helpers (12 fns + 1 struct, ~211 lines): `parse_created_at`, `remaining_session_duration`, `session_time_exhausted`, `extract_bullet_code_value`, `humanize_agent_name`, `extract_saved_session_name`/`_initial_agent`/`_prompt`, `stable_text_fingerprint`, `count_known_session_markdown_artifacts`, `known_session_activity_unix`, `protocol_backup_stats` + `ProtocolBackupStats`, `is_protocol_backup_file_name`, `system_time_to_unix`. ZERO-line diff. lib.rs: 6609 → 6398. |
| **`v0.3.27`** | **Code split batch — extracted `src-tauri/src/session_persistence.rs`.** Pure refactor. Session contract + cost ledger persistence (8 fns, ~88 lines): `session_contract_path`, `cost_ledger_path`, `load_session_contract`, `write_session_contract`, `load_cost_ledger`, `write_cost_ledger`, `append_agent_cost_to_ledger`, `api_provider_from_cli`. ZERO-line diff. lib.rs: 6693 → 6605. |
| **`v0.3.26`** | **Code split batch — extracted `src-tauri/src/editorial_inputs.rs`.** Pure refactor. Agent input preparation + active-agents log context + time-budget anchor (4 fns, ~154 lines): `effective_agent_input`, `prepare_agent_input` (sidecar for prompts > 48 KB), `build_active_agents_resolved_log_context`, `resolve_time_budget_anchor` (B18 from v0.3.18). ZERO-line diff. lib.rs: 6840 → 6688. |
| **`v0.3.25`** | **Code split batch — extracted `src-tauri/src/editorial_prompts.rs`.** Pure refactor. Agent specs (CLI args + spec table) + prompt builders (10 fns, ~245 lines): `claude_args`/`codex_args`/`gemini_args`/`deepseek_args`, `editorial_agent_specs`, `resolve_initial_agent_key`, `ordered_editorial_agent_specs`, `build_draft_prompt`/`build_review_prompt`/`build_revision_prompt`. ZERO-line diff. lib.rs: 7081 → 6840. |
| **`v0.3.24`** | **Code split batch — extracted `src-tauri/src/editorial_helpers.rs`.** Pure refactor. Editorial-session lifecycle helpers (7 items, ~195 lines): `filter_existing_agents_to_active_set`, `resolve_effective_active_agents`, `review_complaint_fingerprint`, `FinalizeRunningArtifactsGuard` (struct + impl + Drop), `finalize_running_agent_artifacts`, `write_editorial_agent_running_artifact`, `write_editorial_agent_error_artifact`. lib.rs: 7270 → 7075. |
| **`v0.3.23`** | **Code split batch — extracted `src-tauri/src/cloudflare.rs`.** Pure refactor. Cloudflare client + D1 + Secrets Store (29 fns + 1 struct, ~960 lines): HTTP layer (`cloudflare_get`/`post_json`/`patch_json`/`get_paginated_results`/`client`/`page_path`/`verify_path`/`token_kind`/`error_summary`), token resolvers, JSON helpers (`CloudflareStoreRecord`, `cloudflare_result_*`, `cloudflare_secret_*`, `cloudflare_created_result_id`), D1+Secrets ensure logic (`ensure_cloudflare_d1_database`, `ensure_cloudflare_secret_store`, `provision_maestro_d1_schema`, `link_secret_store_reference`), AI provider bridge (`ai_provider_secret_values`, `upsert_ai_provider_secrets`, `write_ai_provider_metadata_to_cloudflare`), probe entry (`run_cloudflare_probe`, `probe_row`). Tauri commands `cloudflare_env_snapshot`/`verify_cloudflare_credentials` stay in lib.rs (registry boundary). lib.rs: 8236 → 7260. |
| **`v0.3.22`** | **Code split batch — bundled 3 isomorphic provider runners.** Pure refactor. Closes runner-extraction phase of migration step 3 with all 4 per-provider runners now in dedicated modules. Extracted to `src-tauri/src/provider_runners.rs` (~1100 lines, 20 fns): `run_openai_api_agent`/`run_anthropic_api_agent`/`run_gemini_api_agent`, the unified `write_provider_failure_result` helper family (`api_cost_preflight_result`, `write_provider_missing_key_result`, `write_provider_error_result`, `write_provider_success_result`, `editorial_api_system_prompt`, `log_provider_api_started`), 3 model resolvers (`resolve_openai_model`/`resolve_anthropic_model`/`resolve_gemini_model` + private `choose_preferred_model`/`api_model_ids`/`gemini_model_ids`), and 3 response parsers (`openai_response_text` + private `anthropic_response_text`/`gemini_response_text`/`gemini_usage_tokens`). Followed advisor's "delete first, edit second" sequence. Bundled `.github/FUNDING.yml` URL update. lib.rs: 9351 → 8233. |
| **`v0.3.21`** | **Code split batch — extracted `src-tauri/src/provider_deepseek.rs` (the structural outlier).** Pure refactor. DeepSeek API runner + 4 helpers (`run_deepseek_api_agent`, `write_deepseek_error_result`, `deepseek_model`, `resolve_deepseek_model`, `deepseek_model_ids`) + the `deepseek_model_ids_extract_current_api_shape` test, ~467 lines. Extracted ahead of openai/anthropic/gemini because DeepSeek predates the unified `write_provider_failure_result` family and keeps its own custom error helper. Visibility upgrades in lib.rs: `AiProviderConfig` struct + 4 secret fields, `EditorialAgentResult` struct + all 12 fields, `first_env_value`, `effective_provider_key`, `log_editorial_agent_finished`, `extract_maestro_status`, `api_error_message`. Cross-review session `73efbf6c` 5 rounds → converged READY. lib.rs: 9776 → 9351. |
| **`v0.3.20`** | **Code split batch — extracted `src-tauri/src/provider_retry.rs`.** Pure refactor for migration step 3 ("AI provider credentials/probes"). Migrated `build_api_client`, `send_with_retry`, `parse_retry_after_header`, and the 4 `PROVIDER_RETRY_*` constants (137 lines with doc comments). The 4 provider runner functions (DeepSeek/OpenAI/Anthropic/Gemini) stay in `lib.rs` and will move in v0.3.21+ batches with their request body shapes and response parsers. 74 cargo tests passing, zero regression. lib.rs: 9873 → 9776 lines. |
| **`v0.3.19`** | **Code split batch — extracted `src-tauri/src/logging.rs`.** Pure refactor finishing migration step 2 ("logging and path safety") started in v0.3.17. Migrated `NATIVE_LOG_SEQUENCE` static + `LogSession`/`LogEventInput`/`LogWriteResult` structs + `create_log_session` factory + `write_log_record` (157 lines with doc comments). Sanitization helpers (`sanitize_short`/`sanitize_value`) upgraded to `pub(crate)` for cross-module access. Stayed in `lib.rs`: Tauri command shells (`runtime_profile`, `write_log_event`), panic/crash record helpers, `log_editorial_agent_*` (move with editorial orchestration batch). 74 cargo tests passing (zero regression). lib.rs: 9930 → 9873. |
| **`v0.3.18`** | **B17/B18/B19 production-bug fixes deferred from v0.3.17 split.** B17 saved_contract pre-select on cold-open: `ResumableSessionInfo` now carries `saved_active_agents`/`saved_initial_agent`/`saved_max_session_cost_usd`/`saved_max_session_minutes`; `startResumeSession` applies these to React state and runOptions before invoking, so an operator who paused with DeepSeek-only doesn't see all 4 spawn after a cold app open. B18 TIME_LIMIT_REACHED instant-fire: new `resolve_time_budget_anchor(created_at, is_resume, now)` switches the time-budget gate to `now` on resume; `created_at` still anchors cumulative metrics. Confirmed by operator log 19:43:13 where v0.3.16 fired TIME_LIMIT_REACHED in 1.3s on resume because `created_at` was 5+ days old. B19 stale "Ultimo estado": `filter_existing_agents_to_active_set` filters resume's `existing_agents` to only peers in the current active set, normalizing aliases (Anthropic→claude, OpenAI→codex). 7 new tests = 73 cargo tests green. |
| **`v0.3.17`** | **Code split batch — extracted `src-tauri/src/app_paths.rs`.** Pure refactor (no behavior change) per `docs/code-split-plan.md` migration step 2 ("logging and path safety"). Migrated `APP_ROOT` static + 18 path/safety helpers (`app_root`, `data_dir`, `sessions_dir`, `checked_data_child_path`, `sanitize_path_segment`, etc.) into a self-contained module with doc comments. `initialize_app_root`, `install_process_panic_hook`, and `write_early_crash_record` stayed in `lib.rs` because they touch the Tauri runtime or compose JSON crash records. `lib.rs`: 9759 → 9632 lines. 66 cargo tests still passing; npm typecheck + build clean. |
| **`v0.3.16`** | **Pos-v0.3.15 production-log fixes + Codex NB-2/NB-5 hardenings.** B11 regressao do callsite frontend (resume nao propagava `runOptions`); B13 `send_with_retry` helper aplicado nos 4 providers (1 retry, 1.5s backoff) para PROVIDER_NETWORK_ERROR transitorios; B14 mesma helper respeita `Retry-After` (delta-seconds ou HTTP-date) com default 30s e cap 120s para HTTP 429; B15 contador `consecutive_all_error_rounds` escalava com novo status `ALL_PEERS_FAILING` apos 3 rounds onde todos os peers caem em tone=error. Hardenings: `FinalizeRunningArtifactsGuard` RAII via Drop (cobre panics/early-returns que a hook v0.3.15 nao cobria); novo `clippy.toml` com `disallowed-methods` para `Command::new` + `#![warn(clippy::disallowed_methods)]` em lib.rs (qualquer spawn editorial fora de `hidden_command` agora dispara warn). Clippy hygiene: 2 idiom fixes (`manual_pattern_char_comparison` + `if_same_then_else` no proprio codigo da v0.3.15). 5 testes novos = 66 cargo tests verde. |
| **`v0.3.15`** | **Anti-"casca vazia" sweep — 12 distinct fixes from session-log analysis.** B1 Gemini sandbox trust forced via `GEMINI_CLI_TRUST_WORKSPACE` env; B2 dedicated `AGENT_FAILED_EMPTY` classifier for success+empty review (DeepSeek's frequent 200-OK-empty failure mode); B3 Windows pipe error 109/232/233 classification + diagnostic surfaced in artifact; B4 tail-preserving stderr cap (`truncate_text_head_tail`, head 1 KiB + tail 60 KiB) replaces head-only truncation; B5 aggregator filter routes empty-output statuses through operational failures; B6 `finalize_running_agent_artifacts` rewrites `RUNNING` placeholders at session end (no timeout reintroduced); B7 child-process UTF-8 forced via `PYTHONUTF8`/`LC_ALL`/`LANG`; B8 `#[serde(default)]` on `SessionContract` so legacy contracts no longer fail to parse and overwrite `created_at`; B9 partial — `review_complaint_fingerprint` detects 3-round repeat NOT_READY rebuttals and emits `session.divergence.persistent` warn; B10 UI cap placeholders + tooltips; B11 active_agents wiring (frontend resume now forwards UI selection; backend logs resolution trail via `session.editorial.active_agents_resolved`); B12 cost/time controls audited and confirmed wired (visibility added). Anti-drift refactors: `resolve_effective_active_agents` and `build_active_agents_resolved_log_context` extracted as pure functions so runtime and tests share a single source of decision-tree and NDJSON payload shape. 20 new `#[test]` invariants — 61 cargo tests green. Cross-review-v2 quadrilateral CONVERGED `unanimous_ready` (session `1f259a0e`) after three iterations closed legitimate NEEDS_EVIDENCE / NOT_READY blockers via real code work. |
| **`v0.3.14`** | **Rigorous security/UX audit closure.** Top-level `ErrorBoundary` so render-phase exceptions no longer blank the webview silently; `useEscapeKey` hook wired on the two custom-portal dialogs (`PromptModal`, `ResumeDialog`) that lacked ESC dismissal — mirrors the same fix shipped in admin-app v02.00.00 and mainsite-app v03.22.00. Boundary forwards exceptions to the same `logEvent` NDJSON channel `installGlobalDiagnostics` uses, so the audit trail stays single-source. |
| **`v0.3.13`** | **Session controls, API peers, attachments, and code splitting.** Added selectable peers, optional time/cost caps, UI-managed provider tariffs, real direct-API runners, provider-native attachments, human-readable logs, and lazy PostEditor loading. |
| **`v0.3.12`** | **README organizational standardization.** Adopted the shared repository README opening pattern and added the top-level version-history table while keeping the Windows/Tauri operational details intact. |
| **`v0.3.11`** | **DeepSeek real-peer integration.** Added DeepSeek as an API-backed editorial peer, real model-list verification, and stronger Cloudflare Secrets Store reload behavior. |
| **`v0.3.10`** | **Long-run orchestration reliability.** Fixed broken paused-session rendering, improved visible logs, removed false blocked end states, and reduced very-large prompt pipe failures. |
| **`v0.3.9`** | **Cloudflare persistence and settings maturation.** Continued the secrets/configuration persistence and operational readiness work that led into the current build. |

Maestro is independent from `cross-review-mcp`; it incorporates the same strict convergence discipline in its own application logic. It is designed to run from a folder, keep runtime data out of Git, and store operator protocols, drafts, evidence, and sessions locally under ignored runtime paths.

Target platform: Windows 11+.

Planned modern stack: Tauri 2 + WebView2, React 19, Vite 8, TypeScript 6, Vitest, Biome, ESLint, and lucide-react.

Diagnostic logs are structured NDJSON files under `data/logs/`, one file per app execution, with native/frontend context and per-agent process events so failures can be attached for precise analysis. The app UI shows a human-readable activity summary while the raw NDJSON remains available for deep debugging. See `docs/logging.md`.

CLI agents run in background by design, without visible terminal windows in Windows release builds. DeepSeek and Grok run through official API paths, not local CLIs. Real editorial calls do not have an artificial timeout. The operator can choose Claude, Codex, Gemini, DeepSeek, or Grok to write the first version; that choice is saved with the session. Maestro applies a tribunal-style editorial model: the agent that authored the current draft/revision is the petitioner for that cycle and can revise/contest through the next cycle, but cannot vote as reviewer of the same text. Review rounds use only independent active peers; if the author cannot be verified or no independent reviewer remains, the session pauses instead of allowing self-review. The operator sees friendly progress, elapsed-time heartbeat status, phase status, resume controls, and a selectable UI verbosity level, while raw prompts, stdout, stderr, working drafts, and transcripts stay out of the normal interface and remain protected as ignored local runtime artifacts under `data/sessions/`.

MainSite-bound editing uses a PostEditor parity module, not a generic editor. See `docs/text-editor-decision.md` and `docs/mainsite-compatibility-contract.md`.

First-run dependency checks, authorized background installation, CLI setup, and authentication flows are planned under `docs/runtime-bootstrapper.md`.

CLI adapter feasibility and risks are audited under `docs/cli-agent-audit.md`.

Cloudflare account/token configuration now verifies the token, prepares `maestro_db`, reuses an existing account Secrets Store when present, and creates `maestro` only when no store exists and creation is permitted. Broader API-first D1 publishing remains tracked under `docs/cloudflare-credentials.md`.

Official AI provider API credentials can be saved locally in `data/config/ai-providers.json` and verified against OpenAI, Anthropic, Gemini, and DeepSeek model-list endpoints. API-backed editorial sessions require provider tariffs plus an explicit per-session USD cost limit; paid calls do not run with an implicit or hard-coded unlimited budget. Full SDK orchestration remains tracked under `docs/ai-provider-credentials.md`, alongside the existing CLI path.

Configuration persistence supports three modes: local JSON for everything, Windows env-var hybrid for tokens/API keys plus JSON for other settings, and Cloudflare remote persistence through D1 `maestro_db` plus Cloudflare Secrets Store. See `docs/configuration-persistence.md`.

The portable ZIP includes `LEIAME.md` with first-run instructions for end users, including `data/config/bootstrap.json`, Cloudflare environment variables, and per-execution NDJSON logs.

The growing native and React surfaces now have a staged modularization plan in `docs/code-split-plan.md`.

Prompt-to-consensus sessions export separate final text and session minutes. Interrupted sessions can be resumed from `data/sessions/`; if a new protocol is loaded before resume, Maestro passes it to the agents and preserves the previous protocol as a local session artifact. See `docs/editorial-session-workflow.md`.

Shared chat import, Markdown/PDF support, and Cloudflare D1 integration are planned under `docs/import-export-cloudflare.md`.

Web fetch, curl-compatible replay, web search, rendered collection, and human-assisted browser capture are planned under `docs/web-evidence-engine.md`.

ABNT citation/reference formatting and Maestro's deterministic fourth-peer role are planned under `docs/abnt-citation-engine.md`.

Link checking, sanitization, correction proposals, and cross-review escalation are planned under `docs/link-integrity-engine.md`.

## Day-Zero Security Posture

- No secrets or API keys in the repository.
- GitHub Secret Scanning, Code Scanning, CodeQL, and Dependabot are assumed active.
- CodeQL uses GitHub Default Setup. Advanced Setup requires prior justification and explicit operator authorization.
- Dependabot alert triage is tracked in `docs/dependabot-alert-triage.md`.
- Operator-supplied editorial protocols are imported through the app and stored locally, not committed.
- Public release requires pre-cloud exposure audit and full-history secret scan.

## Release Planning

GitHub Releases, GitHub Packages, GitHub Pages, and GitHub Sponsors are planned from day zero. Releases publish a portable Windows ZIP; GitHub Packages publishes a GHCR/OCI automation mirror, not NuGet. See `docs/release-engineering-plan.md`.

Version tags, changelog headings, and release labels use the `vX.X.X` format.

---

<p align="center"><span style="font-size: 1.5em;"><strong>© LCV Ideas &amp; Software</strong></span><br><sub>LEONARDO CARDOZO VARGAS TECNOLOGIA DA INFORMACAO LTDA<br>Rua Pais Leme, 215 Conj 1713&nbsp;&nbsp;- Pinheiros<br>São Paulo - SP<br>CEP 05.424-150<br>CNPJ: 66.584.678/0001-77<br>IM 05.424-150</sub></p>
